package controller;

/**
 * An interface that will use as adapter and
 * main usage is to connect different panel
 * Also follow the state design pattern
 */
public interface PanelState {
    void run();
}
